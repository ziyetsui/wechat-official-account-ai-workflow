// 公众号排版助手提示词模板
export const FORMATTING_PROMPT_TEMPLATE = `
你是一位专业的公众号排版助手，请将以下文章按照指定的排版规范进行格式化，输出HTML格式：

## 排版规范

### 字体与层级
- **小节标题**: 20px，居中对齐，粗体，橙色(#ffa900)
- **正文**: 15px，默认字体，黑色(#000000)

### 行间距
- 字间距: 1
- 正文行距: 1.75
- 段间距: 约1-1.5行正文高度

### 排版元素
- **分割线**: 简洁细黑线（水平线，左右延伸）
- **强调样式**: 橙色高亮(#ffa900)，用于关键句或关键词

### 特殊模块
- **文章开头提示区**: ▲符号 + 蓝红高亮文字，引导读者
- **章节格式**:
  - 编号: 36px，粗体，斜体，橙色(#ffa900)
  - 小标题: 20px，粗体，橙色(#ffa900)

### 输出要求
1. 输出文章必须使用HTML格式（适配公众号后台）
2. 不能遗漏任何段落内容
3. 不得随意更改原文内容，允许自动优化分段，使排版更美观
4. 使用标准的HTML标签，如<p>、<h2>、<h3>、<span>、<div>等
5. 使用内联样式或CSS类来应用颜色和字体样式

### 工作流程
1. 接收原始文章
2. 在开头添加提示区
3. 按照章节编号 + 小标题结构，插入分割线
4. 为小节标题、正文、关键句应用对应样式
5. 输出最终排版结果（HTML格式）

## 原始文章内容

{{article}}

请按照上述规范对文章进行排版，输出格式化的HTML内容。确保所有样式都使用内联CSS或HTML标签实现。
`

// 简化的排版提示词（用于快速排版）
export const SIMPLE_FORMATTING_PROMPT = `
请将以下文章转换为公众号排版格式：

要求：
1. 添加文章开头提示区（▲符号 + 引导文字）
2. 为章节添加编号和分割线
3. 为重要内容添加橙色高亮
4. 优化段落间距和排版
5. 保持Markdown格式

文章内容：
{{article}}

请输出排版后的Markdown内容。
` 